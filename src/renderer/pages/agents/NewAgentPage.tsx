/**
 * New Agent page - create a new agent with config and instructions
 * Route: /agents/new
 * 
 * Purpose: Guide user through creating a new agent configuration
 * Features: Agent name/model/temperature config, tool selection, 
 *           instructions editor, YAML preview, file generation
 */
export const NewAgentPage = (): React.JSX.Element => (
  <div>
    <h1>Create New Agent</h1>
    <p>Agent creation wizard - placeholder</p>
  </div>
);
